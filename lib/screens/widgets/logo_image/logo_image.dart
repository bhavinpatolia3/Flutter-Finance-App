import 'package:flutter/material.dart';

class LogoImage extends StatelessWidget {
  Widget build(BuildContext context) {
    return Image(
      image: AssetImage('assets/images/image/Logo White.png'),
    );
  }
}
