import 'package:flutter/material.dart';

class ColorLogo extends StatelessWidget {
  Widget build(BuildContext context) {
    return Image(
      image: AssetImage('assets/images/image/Logo.png'),
    );
  }
}
